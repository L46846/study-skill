# Python 基础语法学习笔记

## 1. 变量与数据类型

### 1.1 变量命名规则
- 只能包含字母、数字和下划线，不能以数字开头。
- 区分大小写（`name` 与 `Name` 不同）。
- 避免使用 Python 关键字（如 `if`、`for`、`while`）。

```python
# 正确示例
user_name = "Alice"
age2 = 25
_private = True